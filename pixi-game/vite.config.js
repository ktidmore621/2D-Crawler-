import { defineConfig } from 'vite';

// This base path makes GitHub Pages deployments work for this repo name.
// If your repo name changes, update '/2D-Crawler-/' to match it.
export default defineConfig({
  base: '/2D-Crawler-/',
});
