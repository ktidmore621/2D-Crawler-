import { defineConfig } from 'vite';

// Replace this with your real GitHub repository name later if needed.
// Example: '/my-pixi-game/'
const repoBasePath = '/2D-Crawler-/';

export default defineConfig({
  base: repoBasePath
});
